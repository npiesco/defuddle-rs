[project]
name = "defuddle-demo"
version = "0.1.0"
description = "Defuddle demo orchestration: record, narrate, mux."
requires-python = ">=3.13"
dependencies = [
  "azure-cognitiveservices-speech>=1.43.0",
  "azure-identity>=1.19.0",
]

[tool.uv]
package = false
