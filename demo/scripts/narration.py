NARRATION_TEXT = """\
fsgdb is an MCP-powered code intelligence engine that builds a navigable graph of your entire codebase.
Start with the command line. The stats command shows you exactly what has been scanned and indexed: \
forty-five thousand nodes, two hundred forty-six thousand edges, spanning functions, structs, modules, \
files, and packages across the Servo browser engine.
A graph query finds the most complex Rust functions by cyclomatic complexity — the ones most likely to \
hide bugs or resist refactoring.
No Wi-Fi? No problem. fsgdb runs entirely local. \
Open the browser-based Graph Explorer, connect to your local database, and explore the full \
dependency graph without touching the internet.
Or skip the browser entirely. The native desktop app gives you the same graph and query interface \
as a standalone application — no install required beyond a single binary.
In VS Code, the Cypher Query Editor brings that same graph intelligence directly into your editor. \
Run a query, get a result table, stay in flow.
Open the Graph Visualization and watch the force-directed graph render the full dependency structure \
in real time.
Now hand the graph to an AI agent. The fsgdb MCP server is already wired in.
Ask whether Servo could become a viable Playwright automation target.
The agent reaches straight into the graph: semantic search, module explanation, call chain analysis, \
dependency tracing, test impact.
It finds the WebDriver server, the embedding API, the headless window infrastructure, \
and the web platform test harness.
In under three minutes it maps every automation hook point, traces the JavaScript evaluation path, \
and delivers a structured risk assessment with a clear validation roadmap.
fsgdb: graph-native code intelligence for the agentic era.\
"""
